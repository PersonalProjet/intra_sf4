{% extends 'layout.html.twig' %}

{% block title %}Edit Compte{% endblock %}

{% block body_layout %}
    <h1>Edit Compte</h1>

    {{ include('_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('compte_index') }}">back to list</a>

    {{ include('_delete_form.html.twig', {'id': compte.id}) }}
{% endblock %}