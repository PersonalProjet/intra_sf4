{% extends 'layout.html.twig' %}

{% block title %}Prelevement{% endblock %}

{% block body_layout %}
    <h1>Prelevement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prelevement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prelevement.name }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ prelevement.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prelevement_index') }}">back to list</a>

    <a href="{{ path('prelevement_edit', {'id': prelevement.id}) }}">edit</a>

    {{ include('_delete_form.html.twig', {'id': prelevement.id}) }}
{% endblock %}